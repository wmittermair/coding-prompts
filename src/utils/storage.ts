export class StorageManager {
  static saveToDisk(data: any): void {
    // Speichere in prompts.json
    // Erstelle Backup
  }

  static loadFromDisk(): any {
    // Lade aus prompts.json
    // Fallback auf Backup wenn nötig
  }
} 